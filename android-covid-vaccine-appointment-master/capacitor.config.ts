import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Covid19Vaccine',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
